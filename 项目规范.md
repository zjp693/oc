# OCC 项目规范

## 移动端自适应

- 横向：使用 `rpx + padding + width: 100%`，优先表达左右边距，不把页面级容器宽度写死。
- 纵向：使用 flex 分区，不整页堆固定 `margin-top`。让可变留白由 flex 区域吸收。
- 状态栏：使用 `var(--status-bar-height)` 处理 App 端状态栏安全距离。
- 底部：使用 `env(safe-area-inset-bottom)` 处理底部安全区。
- 表单：宽度自适应，高度固定。输入框、按钮等触控区域可以固定高度。
- 图片：使用 rpx 固定尺寸或 `aspect-ratio` 保持比例，按场景设置 `min/max` 约束。
- 列表页：使用 flex + `min-height`，不要写死整块高度。

